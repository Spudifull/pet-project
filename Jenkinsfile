pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Клонирование кода из GitHub репозитория
                git 'https://github.com/Strevochka/pet-project.git'
            }
        }

    }
}
